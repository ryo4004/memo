import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zatsuzen.memo',
  appName: 'memo',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
